# Origin of the FastAPI backend, for example http://localhost:8000. Required:
# the app reads the address from here only and stops with a clear error if it is
# unset. Restart `npm run dev` after changing this.
VITE_API_URL=
