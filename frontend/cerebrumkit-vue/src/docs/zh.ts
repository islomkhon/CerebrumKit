/**
 * 文档的简体中文版。
 *
 * 结构与 `content.ts` 相同：`DOC_GROUPS` 给出目录分组名，`DOC_SECTIONS`
 * 为每个锚点提供一条记录。`id` 保持英文——它是与 `docs/index.ts` 的约定，
 * 也是 `#doc-overview` 这类链接的地址，不能翻译。
 */
import type { DocGroup, DocSection } from './content'

export const DOC_GROUPS: DocGroup[] = [
  { label: '快速上手', ids: ['overview', 'quick-start', 'concepts'] },
  {
    label: '工作原理',
    ids: [
      'run-lifecycle',
      'building-a-project',
      'workflow',
      'agents',
      'skills',
      'tools',
      'context-tools',
      'storage',
      'memory',
      'delegation',
    ],
  },
  { label: '运营与维护', ids: ['chats', 'users-and-access', 'llm-connections', 'system-project'] },
  { label: '参考', ids: ['use-cases', 'security', 'operations', 'reference'] },
]

export const DOC_SECTIONS: DocSection[] = [
  {
    id: 'overview',
    title: '概览',
    body: [
      'CerebrumKit 是智能体项目的起点。它提供一个管理后台，在这里用**技能**和**工具**把智能体组装起来；一个存储库，用来存放可以交给这些智能体的业务表；以及一个客户端面板，项目所服务的人在那里与智能体对话。',
      '',
      '这里没有任何属于特定行业的东西。没有商品目录，没有订单，没有产品表。这里有的是每个这类项目都需要的机制，因此新项目从「智能体该做什么」开始，而不是从「我该怎么跑起一个智能体循环」开始。',
      '',
      '## 你得到什么',
      '',
      '| 组成部分 | 是什么 |',
      '| --- | --- |',
      '| 项目 | 组织成员、智能体及其可用的存储表，并持有决定由谁回答消息的工作流 |',
      '| 智能体 | 名称、充当系统提示词的描述，以及它携带的技能 |',
      '| 技能 | 书面指令，加上这些指令需要的工具 |',
      '| 工具 | OpenAI 函数规格加上 Python 函数体，两者都保存在数据库中 |',
      '| 存储 | 在面板中创建的表，支持 Excel 导入与导出 |',
      '| 聊天 | 每个聊天一个 WebSocket，驱动智能体循环并流式返回回复 |',
      '| 记忆 | 智能体关于某个用户写下的笔记，在该用户的其他聊天中可读 |',
      '| 委派 | 一个智能体把独立的任务交给项目中的另一个智能体 |',
      '| 用户与角色 | `admin` 搭建项目，`client` 只能看到自己的 |',
      '| 系统项目 | 受保护的项目，其中的 Project Manager 智能体负责管理整套部署 |',
      '',
      '## 一句话说明原理',
      '',
      '一条消息通过聊天的 WebSocket 到达。智能体循环读取项目工作流，选出应该作答的智能体或分组，用智能体的描述、它的技能以及预运行工具的输出拼出系统提示词，然后调用模型。工具调用以 function call 的形式返回；每个调用都针对数据库中的工具函数体执行，并注入项目、聊天、智能体和用户 id，因此工具只能触碰自己这一次运行的数据。结果返回给模型，模型产出的文本被广播到聊天中。',
      '',
      '由于工具和技能的定义保存在数据库而不是代码里，在面板中修改它们会在下一次运行时生效——无需重启，也无需部署。',

      '',
      '## Author',
      '![Islomkhon Nizomkhonov](/IslomkhonNizomkhonov.png)',
      '',
      '### Islomkhon Nizomkhonov（小伊）',
      '',
      '- **教育背景**：中国矿业大学计算机科学硕士',
      '- **主要技能**：软件工程、深度学习、机器学习、数据科学、物联网、预测性维护',
      '',
      'Islomkhon Nizomkhonov 于 1994 年出生于乌兹别克斯坦塔什干。他是一名高级软件工程师，在机器学习与预测性维护领域拥有六年以上经验。他曾服务于多个行业和大型企业，包括 [XCMG（徐工集团）](https://www.xcmg.com/) 和 [Uztelecom（乌兹别克斯坦国家电信运营商）](https://uztelecom.uz/)，为其提供基于深度学习技术的解决方案。他的研究聚焦于利用神经网络进行工业物联网系统的故障诊断。目前他主要关注 **AI 智能体**、**面向 AI 与借助 AI 的软件工程** 以及 **可信 AI**。如需合作或探讨应用场景，欢迎通过邮箱 [islomkhon@126.com](mailto:islomkhon@126.com) 或电话 +86 139 1487 4257 与他联系。',
    ].join('\n'),
  },
  {
    id: 'quick-start',
    title: '快速开始',
    body: [
      '## 环境要求',
      '',
      '- Python 3.12+ 与 Node 20+',
      '- PostgreSQL；或者保持 `DATABASE_URL` 为默认值，用 SQLite 快速体验',
      '',
      '## 1. 数据库',
      '',
      '```bash',
      'docker compose up -d              # 或者把 DATABASE_URL 指向已有服务器',
      'createdb cerebrumkit              # 自己运行 Postgres 时',
      '```',
      '',
      '## 2. 后端',
      '',
      '```bash',
      'cd backend',
      'python -m venv venv',
      'venv/Scripts/python.exe -m pip install -r requirements.txt      # Windows',
      '# source venv/bin/activate && pip install -r requirements.txt   # macOS / Linux',
      'cp .env.example .env              # 然后设置 DATABASE_URL、SECRET_KEY、',
      '                                  # DEEPSEEK_* 和 SEED_* 账号',
      'venv/Scripts/python.exe seed_all.py',
      '```',
      '',
      '`seed_all.py` 会创建数据表、国家列表、管理员与客户端账号、通用工具和技能，以及带有 Project Manager 智能体的系统项目。每一步都是幂等的，重复运行是安全的。',
      '',
      '它还会把 `DEEPSEEK_*` 复制进 `platforms` 表——系统实际运行靠的就是这张表。之后，**设置 -> LLM 连接**就是修改服务商、基础 URL、API 密钥或模型的地方。',
      '',
      '## 3. 前端',
      '',
      '```bash',
      'cd ../frontend/cerebrumkit-vue',
      'npm install',
      'npm run dev',
      '```',
      '',
      '在 Windows 上也可以双击 `run.ps1`，它会同时启动两个服务；`stop.ps1` 用于停止。',
      '',
      '## 4. 登录',
      '',
      '`seed.py` 会创建两个账号，两者用同一个密码登录：',
      '',
      '| 账号 | 邮箱 | 可访问范围 |',
      '| --- | --- | --- |',
      '| 管理员 | `SEED_ADMIN_EMAIL` | 整个管理后台 |',
      '| 客户端 | `SEED_CLIENT_EMAIL` | 只限于分配给该用户的项目 |',
      '',
      '代码中没有内置的默认账号。这三个值都来自 `backend/.env`，只要有一个为空，`seed_all.py` 就会报错停止，因此全新克隆的代码里没有可以被猜到的登录信息：',
      '',
      '```bash',
      'SEED_ADMIN_EMAIL=admin@example.com',
      'SEED_CLIENT_EMAIL=client@example.com',
      'SEED_PASSWORD=你自己设定的密码',
      '```',
      '',
      '打开 <http://localhost:5173>，用管理员邮箱和该密码登录。之后要改密码，在 **管理后台 -> 用户** 中编辑该用户即可；重新运行种子脚本不会改动已存在的账号。',
      '',
      '> `SEED_PASSWORD` 是两个账号共用的同一个密码，也是攻击者最先尝试的组合。在别人能访问这套部署之前，先改掉它。',
      '',
      '## 5. 指向一个模型',
      '',
      '打开 **设置 -> LLM 连接**。种子脚本从 `.env` 复制过来的那条连接已经是激活状态，并标记为**使用中**。点击**测试**：它会发起一次很小的真实调用，因此错误的 URL、密钥或模型名会在这里暴露，而不是在运行中途。',
      '',
      '## 6. 第一次对话',
      '',
      '打开**项目**，选择 **System** 项目，打开一个聊天，随便问 Project Manager 一些问题。如果它能回答，说明整套部署已经端到端跑通。',
      '',
      '想做自己的东西，请看下面的**搭建一个项目**。或者直接让 Project Manager 帮你做——这正是系统项目存在的意义。',
    ].join('\n'),
  },
  {
    id: 'concepts',
    title: '核心概念',
    body: [
      '这一页剩下的内容，都是这些概念中某一个的细节。',
      '',
      '| 概念 | 是什么 |',
      '| --- | --- |',
      '| **项目** | 工作的单位。拥有一个工作流、一组成员用户、一组智能体和一组存储表。 |',
      '| **智能体** | 名称、会成为系统提示词的描述，以及它携带的技能。在所属项目的聊天中作答。 |',
      '| **技能** | 一段书面指令，加上这段指令需要的工具。把技能挂到智能体上，就给了它这些工具。 |',
      '| **工具** | 函数规格（模型看到的部分）加 Python 函数体（真正执行的部分）。两者都是数据库中的行。 |',
      '| **上下文工具** | 智能体在读取消息*之前*运行的工具，其输出会放进系统提示词。 |',
      '| **工作流** | 由 start / group / stop 构成的图，决定哪些智能体按什么顺序回答消息。 |',
      '| **存储表** | 从面板创建的真实数据库表，表和每个列都带描述。 |',
      '| **聊天** | 用户与项目智能体之间的一次对话，通过 WebSocket 驱动。 |',
      '| **记忆** | 智能体关于某个用户写下的笔记，以（智能体，用户）为键，因此在该用户的各个聊天中都可读。 |',
      '| **LLM 连接** | `platforms` 中的一行：基础 URL、API 密钥、模型、温度、最大 token、思考。同一时间只有一条激活。 |',
      '| **系统项目** | 唯一不能改名或删除的项目。Project Manager 就在其中。 |',
    ].join('\n'),
  },
  {
    id: 'run-lifecycle',
    title: '一条消息如何变成回答',
    body: [
      '这就是整个循环，按顺序排列。每一步都给出了执行它的文件。',
      '',
      '1. **用户发送消息**，通过聊天的 WebSocket（`backend/app/routers/websocket.py`）。系统会校验 token，而且用户必须确实拥有这个聊天——只有有效 token 是不够的。消息作为后台任务交给智能体循环，因此一次运行永远不会阻塞套接字。',
      '2. **循环加载项目工作流**（`backend/app/core/agent_loop.py`）。它找到 `start` 节点，然后沿图前进。单次运行上限为 200 步，用来兜底畸形或成环的工作流。',
      '3. **分组节点依次运行它的智能体**，按分配顺序一个一个来。缺失的智能体会被跳过并记录警告，而不是白白花掉一次调用。',
      '4. **每个智能体的提示词由四部分组成**：`You are {name}. {description}`，其中描述*就是*该智能体的常驻简报；它的**上下文工具**此刻的输出，各自带标题（总计上限 24,000 字符）；该智能体在这个聊天中的**上一条回复**，作为新消息之前的一轮放入，让追问接着之前的工作而不是从零开始（取尾部 2,000 字符）；以及收到的消息。',
      '5. **调用模型**，走系统当前激活的 LLM 连接。只要背后的配置没变，客户端就会被复用，因为重建一个会丢掉它的 HTTP 连接池。',
      '6. **执行工具调用。** 每次调用都会先校验项目、聊天、智能体和工具，然后编译函数体并调用，此时 `project_id`、`chat_id`、`agent_id`、`user_id` 和 `call_depth` 已在作用域内（`backend/app/core/tool_exec.py`）。',
      '7. **结果返回给模型。** 超过 8,000 字符的结果是在 JSON *内部*裁剪，而不是直接截断，因为半个 JSON 对象会让模型重发同一个调用而不是作答。完整结果仍然保留在聊天里的工具卡片上。',
      '8. **回复被保存并广播。** 工具调用在记录中显示为各自的卡片，智能体的文本流式进入对话。',
      '9. **下一个节点开始运行**，输入是上一组的合并输出。最后一组回答用户。',
      '',
      '运行可以在中途从客户端停止；任务被取消，聊天回到空闲状态。',
      '',
      '## 智能体看得到什么、看不到什么',
      '',
      '- 它**不会**拿到之前的聊天记录（`HISTORY_WINDOW` 为 `0`）。',
      '- 它**会**拿到自己的上一条回复，加上上下文工具取到的内容，以及运行期间工具返回的一切。',
      '- 跨聊天回忆靠的是**记忆**：智能体用 `memory_write` 写下笔记，之后与同一用户的任何聊天里都能用 `memory_lookup` 读回来。',
      '',
      '这种设计是刻意的。无限制的聊天记录会挤掉工具和当前请求，而智能体通常需要的是*它了解到的一个事实*，而不是对话的措辞。',
    ].join('\n'),
  },  {
    id: 'building-a-project',
    title: '搭建一个项目',
    body: [
      '可以在面板里搭建（**项目 -> 创建项目**），也可以在系统项目的聊天里让 Project Manager 帮你搭建。有效的顺序是：',
      '',
      '1. **存储** —— 创建项目拥有的表，给表和每个列都写上描述。工具会读取这些描述，智能体也能看到。',
      '2. **工具** —— 智能体应该能做的一件事对应一个工具。你会写 Python 就自己写函数体，否则描述这个工具，让生成器起草规格。',
      '3. **技能** —— 把工具归到书面指令之下：什么时候用、传什么、怎么读结果。',
      '4. **智能体** —— 一段作为智能体常驻简报的描述，加上它携带的技能。可选地，还可以配置每条消息之前运行的上下文工具。',
      '5. **项目** —— 分配表、加入智能体，并把工作流接好，让消息能到达它们。',
      '6. **用户** —— 创建客户端账号并把它们加入项目。它们只能看到自己的项目和聊天。',
      '',
      '两个值得养成的习惯：',
      '',
      '- **把描述当成写给新同事看的。** 表、列、工具和技能的描述是模型了解你数据的唯一途径。「以欧元计的客户生命周期价值，每晚更新」值十行代码；「一个数字」一文不值。',
      '- **一个智能体只干一件事。** 带四个技能、二十个工具的智能体，每件事都做得比四个各带一个技能的智能体差，因为每个工具描述都在争夺模型的注意力。',
    ].join('\n'),
  },
  {
    id: 'workflow',
    title: '工作流',
    body: [
      '工作流是以 JSON 形式保存在项目上的一张小图。它有三种节点：',
      '',
      '| 节点 | 作用 |',
      '| --- | --- |',
      '| `start` | 消息的入口。有且只有一个。 |',
      '| `group` | 按顺序依次运行分配给它的智能体。 |',
      '| `stop` | 结束这条路径。 |',
      '',
      '`wires` 把它们连起来。一个分组的合并输出会传给后面的节点，因此第二个分组把第一个分组的成果当作自己的输入。回答给用户的是**最后一个**分组的输出。',
      '',
      '```json',
      '{',
      '  "nodes": [',
      '    { "id": 1, "type": "start", "label": "Start" },',
      '    { "id": 2, "type": "group", "label": "Default Group",',
      '      "agents": [ { "id": 1, "name": "Project Manager" } ] },',
      '    { "id": 3, "type": "stop", "label": "Stop" }',
      '  ],',
      '  "wires": [',
      '    { "id": "1-out->2-in", "fromNode": 1, "fromPort": "1-out", "toNode": 2, "toPort": "2-in" },',
      '    { "id": "2-out->3-in", "fromNode": 2, "fromPort": "2-out", "toNode": 3, "toPort": "3-in" }',
      '  ]',
      '}',
      '```',
      '',
      '## 串联智能体',
      '',
      '两个分组串联起来就是一条流水线：一个负责收集的研究智能体，接着一个起草的写作智能体，再接着一个检查的审核智能体。每个分组都能看到前一个分组的产出。',
      '',
      '把链子保持短。每个分组都是一次完整的模型往返，有自己的工具预算，所以五段链子就是五次丢失线索的机会和五倍的延迟。',
    ].join('\n'),
  },
  {
    id: 'agents',
    title: '智能体',
    body: [
      '智能体是一个名称、一段描述和一组技能。',
      '',
      '**描述就是系统提示词。** 它不是文档。把它写成一页常驻简报：这个智能体是谁、负责什么、应该如何表现、应该拒绝什么。循环会把它包成 `You are {name}. {description}`，并追加一句简短的指示：要乐于助人、简洁、少用工具。',
      '',
      '**技能带来工具。** 智能体没有直接的工具列表；它有技能，每个技能携带工具。所以增加一项能力意味着挂上一个技能，而不是勾十二个复选框。',
      '',
      '**上下文工具**（可选）是智能体在读取消息之前自行运行的工具。把它们用于那些事先可知、且总是需要的东西——当前日期、用户画像、商品目录。每个结果都会放在你写的标题之下，进入系统提示词。参见下面的**上下文工具**。',
      '',
      '**启用中**只是一个开关：未启用的智能体会被跳过，它的技能也无法通过它使用。',
      '',
      '## 一个好的描述',
      '',
      '```text',
      '你是一家网店的客服智能体。你回答关于订单、配送和退货的问题。',
      '用 order_lookup 查询订单——绝不要向客户索要你本可以从其账号中找到的',
      '订单号。如果客户要求退款，先核对退货期限再作承诺。如果问题涉及的',
      '商品不在目录里，如实说明，并提出转交给同事。',
      '```',
      '',
      '注意它做了什么：点名工具、划定边界、说明卡住时该怎么办。写成「乐于助人的客服智能体」只会得到一个乐于助人却会编造订单号的智能体。',
    ].join('\n'),
  },
  {
    id: 'skills',
    title: '技能',
    body: [
      '技能就是一段书面指令，加上这段指令需要的工具。把技能给智能体，工具也随之而来。',
      '',
      '技能文本是 markdown，会放进智能体的系统提示词。它适合放那些塞不进智能体描述里的流程性知识：',
      '',
      '- **什么时候用这个技能** —— 触发条件，让智能体在正确的时机选它。',
      '- **指令** —— 按顺序的步骤。',
      '- **示例** —— 一次调用，以及一个好结果长什么样。',
      '- **注意事项与坑** —— 否则智能体每次都会犯的那个错。',
      '',
      '种子技能是很好的模仿对象。**「System Administration」**把 `manage_*` 工具和它们的使用规则打包在一起；**「Delegate a Self-Contained Task to Another Agent」**则是这个格式的完整范例。',
      '',
      '技能可以跨智能体、跨项目复用。如果两个智能体需要同一套流程，就只写一次。',
    ].join('\n'),
  },
  {
    id: 'tools',
    title: '工具',
    body: [
      '一个工具有两半，都保存在工具这一行上：',
      '',
      '| 字段 | 内容 |',
      '| --- | --- |',
      '| `description` | OpenAI 函数规格的 JSON：名称、模型读到的描述，以及参数 |',
      '| `body` | 真正运行的 Python，定义一个与规格同名的函数 |',
      '',
      '面板上的**生成**按钮会根据一段自然语言描述起草这两部分，使用的是设置中配置的模型。请审阅它写出的内容——尤其是函数体，见下面的安全提示。',
      '',
      '## 函数规格',
      '',
      '```json',
      '{',
      '  "type": "function",',
      '  "function": {',
      '    "name": "order_lookup",',
      '    "description": "Look up one order by its id. Returns status, items and delivery date.",',
      '    "parameters": {',
      '      "type": "object",',
      '      "properties": {',
      '        "order_id": { "type": "integer", "description": "The order id." }',
      '      },',
      '      "required": ["order_id"]',
      '    }',
      '  }',
      '}',
      '```',
      '',
      '为模型写描述，而不是为人写：说明它返回什么，也说明它*不*做什么，这样模型就不会误用它。',
      '',
      '## 函数体',
      '',
      '函数体定义一个函数，其名称必须与 `function.name` 一致。运行时，这次运行的各个 id 已在作用域中：',
      '',
      '| 变量 | 是什么 |',
      '| --- | --- |',
      '| `project_id` | 这次运行所属的项目 |',
      '| `chat_id` | 这次运行所属的聊天 |',
      '| `agent_id` | 调用该工具的智能体 |',
      '| `user_id` | 聊天的**所有者**，而不是打字的人：管理员可以在别人的聊天里发言 |',
      '| `call_depth` | `0` 表示由人发起的运行，`1` 表示被委派的运行，依此类推 |',
      '',
      '这些由服务器提供，绝不能声明为参数——能传入 `project_id` 的模型就能读取任何项目。',
      '',
      '```python',
      'def order_lookup(order_id: int):',
      '    from app.core.database import SessionLocal',
      '    from sqlalchemy import text',
      '',
      '    db = SessionLocal()',
      '    try:',
      '        row = db.execute(',
      '            text("SELECT id, status, delivered_at FROM orders "',
      '                 "WHERE id = :oid AND project_id = :pid"),',
      '            {"oid": order_id, "pid": project_id},',
      '        ).mappings().first()',
      '        if row is None:',
      '            return f"No order {order_id} in this project."',
      '        return dict(row)',
      '    finally:',
      '        db.close()',
      '```',
      '',
      '两条让工具可靠的规则：',
      '',
      '- **返回一句话，而不是抛异常。** 抛出的错误会变成模型读到的一段文本；捕获它，你才能说明*为什么*失败。',
      '- **把每个查询限制在这次运行的范围内。** 按 `project_id`（以及用户）过滤，工具就只能碰到交给它的数据。',
      '',
      '> **安全。** 工具函数体是任意 Python，带完整内置函数编译并执行。它刻意不做沙箱隔离，因为自带的工具会导入 `requests`、SQLAlchemy 和应用内部模块。后果是：编写工具等同于在服务器上执行代码。因此工具的接口必须只对管理员开放，并且要像审阅源码一样审阅生成出来的函数体。',
    ].join('\n'),
  },
  {
    id: 'context-tools',
    title: '上下文工具',
    body: [
      '智能体需要的一些信息，在读消息之前就已经知道：它运行的环境、用户画像、它所对照的目录。这些查询不依赖消息的措辞，所以交给模型去做既多花一轮工具，又会在模型觉得自己已经知道时被跳过。',
      '',
      '**上下文工具**把这类查询变成智能体启动方式的一部分。工具会运行，输出会放在你写的标题之下，进入智能体的系统提示词。',
      '',
      '只能选择智能体通过技能已经拥有的工具。对每一个你可以设置：',
      '',
      '| 设置 | 含义 |',
      '| --- | --- |',
      '| 备注 | 写在输出上方的标题，让智能体知道自己看的是什么 |',
      '| 参数 | 需要参数的工具所用的 JSON 对象；不需要参数的留空 |',
      '| 顺序 | 在提示词中谁排在前面 |',
      '',
      '整个块的上限是 24,000 字符。放不下的条目会被丢弃并计数，而不是切成两半，因为半个 JSON 对象会诱使模型再调用一次工具来补齐。',
      '',
      '谨慎使用上下文工具。它们在**每一条**消息上都会运行，因此慢的那个会给每一轮都加上延迟，大的那个会与真正的请求争夺模型的注意力。',
    ].join('\n'),
  },  {
    id: 'storage',
    title: '存储',
    body: [
      '存储是你从面板创建的、无需编写迁移的业务表库。每张表都是数据库中真实的表。',
      '',
      '一张表带有：',
      '',
      '- 一个**名称**，以及智能体可以读取的**描述**。',
      '- **列**，每一列都有类型和描述。',
      '- 一组它被分配到的**项目**。工具只能访问其项目拥有的表。',
      '',
      '列的添加和修改都在面板里完成，底层表会随之改变。行可以手工编辑，表可以**从 Excel 导入**并**导出到 Excel**：导入会预览前几行，让你在确认之前看到映射关系。',
      '',
      '## 描述为什么重要',
      '',
      '智能体永远看不到你的表结构。它看到的是表描述和列描述，并据此决定是否查询、如何查询。一张叫 `t1`、列为 `val` 的表对它来说不可见；`customer_orders` 配上 `delivered_at —— 包裹送达客户的时间；在途时为 NULL` 才是可用的。',
      '',
      '## 它处在什么位置',
      '',
      '给项目分配它的表，然后写读写这些表的工具。这是真实项目常见的样子：业务数据住在存储里，工具则是允许智能体对它执行的动作。',
      '',
      '在面板之外创建的表（添加 SQLAlchemy 模型并运行 `migrate.py`）也可以从存储界面分配到项目。',
    ].join('\n'),
  },
  {
    id: 'memory',
    title: '记忆',
    body: [
      '记忆是智能体在聊天之间记住关于某个人的事实的方式。',
      '',
      '笔记以**（智能体，用户）**为键，而不是按聊天。因此客服智能体在周一写下的笔记，同一个智能体在周五能读到——而另一个智能体读不到；这很重要，因为智能体职责不同，值得记住的东西也不同。',
      '',
      '干活的是两个工具：',
      '',
      '| 工具 | 作用 |',
      '| --- | --- |',
      '| `memory_write` | 保存或更新关于当前用户的笔记 |',
      '| `memory_lookup` | 读回关于当前用户已知的内容 |',
      '',
      '两者都随种子技能**「Context & Memory Awareness」**一起提供。把它挂到任何需要把知识带下去的智能体上。',
      '',
      '记忆不是聊天记录。它用于持久的事实——一个偏好、一条约束、一个悬而未决的问题——由智能体决定什么值得留下。对话本身，智能体有自己的上一条回复；其他一切，它去查询。',
    ].join('\n'),
  },
  {
    id: 'delegation',
    title: '委派',
    body: [
      '智能体可以把一项独立的任务交给同一项目中的另一个智能体，并使用它的回答。',
      '',
      '工具是 `call_agent`，随种子技能**「Delegate a Self-Contained Task to Another Agent」**一起提供——那个技能本身也是这套做法最好的文档，请到技能界面读一读。',
      '',
      '关键特性是：**另一个智能体看不到你对话中的任何内容。** 既看不到用户的消息，也看不到你的工具，更看不到你查到了什么。因此你传入的 `message` 必须承载整个请求——背景、交付物、约束、期望格式。',
      '',
      '让它保持可控的几道防线：',
      '',
      '- 委派最多**深入 3 个智能体**。两个互相调用的智能体会停下，而不是永远循环。',
      '- 被委派的运行获得的工具预算更小（**8 轮**），因为它必须在调用者的这一轮之内作答。',
      '- 没能在时限内完成的委派运行会返回一句解释，调用者像读其他工具结果一样读它。',
      '',
      '委派不是特权，也不属于系统管理。项目里任何智能体都可能需要把工作交给别人，所以谁需要就把技能给谁。',
    ].join('\n'),
  },
  {
    id: 'chats',
    title: '聊天与客户端面板',
    body: [
      '一个聊天属于一个用户和一个项目。驱动循环的是它的 WebSocket：客户端发送消息，服务器流式返回状态、正在输入提示、工具卡片和回复。',
      '',
      '在记录里你会看到：',
      '',
      '- **用户消息**，只出现一次。',
      '- 每次工具调用一张**工具卡片**，带输入和输出——因此错误答案可以追溯到产生它的那次调用。',
      '- **流转消息**：当一个智能体的输出成为另一个的输入时，这次交接也会被记录。它们被标记为流转，而不是伪装成人打出来的。',
      '- **智能体的回复**。',
      '',
      '运行可以中途**停止**；聊天回到空闲状态，已经保存的部分工作留在记录里。',
      '',
      '## 客户端面板',
      '',
      '客户端只能看到**自己的项目和聊天，别的什么都看不到**。他们看不到自己所用功能背后的工具、技能、工作流或存储表——只看到对话。这种分离正是要点：运营者可以在底下重建整个智能体栈，而使用者除了答案变好之外什么都察觉不到。',
    ].join('\n'),
  },
  {
    id: 'users-and-access',
    title: '用户、角色与权限',
    body: [
      '有两种角色。',
      '',
      '| 角色 | 能做什么 |',
      '| --- | --- |',
      '| `admin` | 一切：项目、智能体、技能、工具、存储、用户、LLM 连接。能看到每个聊天。 |',
      '| `client` | 自己的项目和自己的聊天。别的都不行。 |',
      '',
      '用户在**用户**里创建（或者让 Project Manager 在聊天里创建）。客户端只能进入被加入的项目。',
      '',
      '值得了解的访问规则：',
      '',
      '- **公开自助注册永远创建客户端。** 角色取自服务器，绝不从请求体读取——否则任何人都能给自己开一个管理员账号。',
      '- **提升权限只能通过管理员接口授予。**',
      '- **聊天属于它的所有者。** 非管理员不能往别人的聊天里发消息、订阅它，或取消它的运行。',
      '- **匿名请求返回 401，角色不符返回 403。** 每个管理员路由都在路由层面做了保护，因此没有哪个单独的接口能漏掉检查。',
      '- **登录和注册有频率限制。**',
      '',
      '> 管理员*可以*往客户端的聊天里发消息。但运行时仍然代表聊天的所有者：工具里的 `user_id` 是所有者，而不是打字的管理员。',
    ].join('\n'),
  },
  {
    id: 'llm-connections',
    title: 'LLM 连接',
    body: [
      '整个系统运行的模型是 `platforms` 表里的一行，而不是配置文件里的一个值。这让更换服务商、基础 URL、密钥或模型变成管理员在面板里就能完成的数据修改，而不是一次部署。',
      '',
      '**设置 -> LLM 连接**负责管理它们。每一行包含：',
      '',
      '| 字段 | 含义 |',
      '| --- | --- |',
      '| 名称 | 一个标签，方便区分两行 |',
      '| 基础 URL | API 地址（留空则用服务商默认值） |',
      '| API 密钥 | 凭据；按输入原样保存和显示 |',
      '| 模型 | 要请求的模型名 |',
      '| 温度 | `0`–`2`；留空则交给服务商 |',
      '| 最大 token | 回复的上限；留空表示不限制 |',
      '| 思考 | 让推理模型在作答前先思考 |',
      '',
      '## 如何选择',
      '',
      '1. 使用标记为**激活**的那一行。',
      '2. 如果没有激活的行，使用**第一**行。',
      '3. 如果表是空的，使用 `.env` 里的 `DEEPSEEK_*`——全新安装、还没有保存任何东西时就是这样。',
      '',
      '同一时间只有一行处于激活状态：激活一行会清除其他行。页面顶部的横幅始终显示实际生效的是哪条连接，因此你不必自己去应用回退规则。',
      '',
      '## 测试',
      '',
      '**测试**会针对该行发起一次很小的真实调用，并报告返回了什么。这是检查密钥、URL 或模型名最快的方式——每次修改之后都值得点一下，否则坏掉的连接会在很久以后表现为一次失败的运行。',
      '',
      '保存的修改会在**下一次运行**时生效，无需重启。只要背后的配置没变，客户端就会被复用，所以切换服务商不会白白丢掉连接池。',
      '',
      '> API 密钥按输入原样保存、原样返回——不做掩码。设置界面只对管理员开放正是因为这个原因：任何能读到这个列表的人，都能用背后的账户花钱。',
    ].join('\n'),
  },  {
    id: 'system-project',
    title: '系统项目与 Project Manager',
    body: [
      '有一个项目是特殊的。它被标记为 `is_system`，这意味着它自己那一行不能改名也不能删除——这样住在里面的智能体永远有个家。它*内部*的一切仍然可编辑，这正是这套安排能成立的原因。',
      '',
      '它承载 **Project Manager** 智能体，带着**「System Administration」**技能和委派技能。它的工具是：',
      '',
      '| 工具 | 管理什么 |',
      '| --- | --- |',
      '| `manage_users` | 列出、读取、创建、更新、启用或停用用户 |',
      '| `manage_projects` | 项目、它们的成员、它们的智能体和它们的工作流 |',
      '| `manage_agents` | 智能体及其携带的技能 |',
      '| `manage_skills` | 技能及其携带的工具 |',
      '| `manage_tools` | 工具，包括根据描述生成规格和函数体 |',
      '| `manage_storage` | 存储表、它们的列和行 |',
      '| `call_agent` | 把任务交给项目中的另一个智能体 |',
      '',
      '因此，整套部署可以**在聊天里让它来做**。创建用户、建起项目、编写工具、接好工作流——Project Manager 走的是面板用的同一套服务层，而不是 REST API。',
      '',
      '这也是了解这套系统最快的方式：问它现在有什么，然后看它返回哪些工具调用。',
      '',
      '值得了解的防线：Project Manager 不能删除系统项目，不能把自己从里面移出，也不能剥夺自己的主项目或自己的技能——所以你不可能不小心把自己锁在管理之外。',
    ].join('\n'),
  },
  {
    id: 'use-cases',
    title: '使用场景',
    body: [
      'CerebrumKit 是一个起点，所以这些是形态而不是成品。每一个都是同一套机制，配不同的存储库和不同的工具集。',
      '',
      '## 1. 用你自己的数据作答的客服台',
      '',
      '**存储**里放 `customers`、`orders` 和 `articles`。**工具**按 id 查订单、按邮箱找客户、搜索帮助文章。客服智能体携带一个技能，告诉它：先确认客户是谁；绝不要报出一个你没有从订单里读到的送达日期；任何涉及退款的事都升级给人。',
      '',
      '客户端面板就是你的客服收件箱。真正有价值的是工具描述和技能文本，而不是模型：一个普通模型加上你的订单表，胜过一个没有数据的好模型。',
      '',
      '## 2. 架在业务系统之上的操作台',
      '',
      '你的真实数据在另一个系统里，藏在 HTTP API 后面。种子工具 **`api_call`** 负责发请求；为每个接口写一个薄工具——`get_invoice`、`list_open_tickets`、`approve_leave`——这样智能体面对的是有名字的动作，而不是裸 URL。',
      '',
      '工作流里串两个智能体：一个收集并总结，一个起草动作。在任何人批准之前，由人在聊天里读草稿。先只给只读工具；等你开始信任那些描述了，再补上写入路径。',
      '',
      '## 3. 内部销售：打电话之前先做功课',
      '',
      '一个研究智能体，带 `web_search` 技能和架在 `accounts` 表上的 `company_lookup` 工具。它的**上下文工具**在每条消息之前预加载客户记录，于是「我该从什么话题开场？」是针对真实数据回答的，而不是反问回去。',
      '',
      '这里委派也物有所值：主智能体把「总结最近两个季度」交给研究智能体并使用答案，而研究智能体看不到整条对话。',
      '',
      '## 4. 后台单据录入',
      '',
      '操作员把提取出的单据文本粘贴或上传到聊天里。工具拿它去比对存储中的参照表，并提出会计分录。审批队列不过是第二个项目，它的智能体被允许写入。',
      '',
      '存储表上的 Excel 导入是把参照数据送进来的方式；导出则是审阅者把结果带走的方式。',
      '',
      '## 5. 面向你自己客户的产品化助手',
      '',
      '一个客户一个项目，各有自己的存储表和自己的智能体，客户端账号被限定在自己的项目内。管理后台是你的中台；客户端面板就是产品本身。',
      '',
      '因为客户端只能看到自己的聊天，永远看不到背后的工具或表，所以你可以在两次会话之间重建智能体的技能，客户除了答案变化之外什么都注意不到。',
      '',
      '## 6. 用对话管理这个平台',
      '',
      '哪怕完全没有业务领域，系统项目也很有用：让 Project Manager 创建一个用户、建起一个项目、根据描述写出一个工具、把它挂到一个技能上、再接入工作流。然后读记录里的工具卡片，看它究竟做了什么。',
      '',
      '对任何学习这套系统的人来说，这是最快的导览——而且它本身就是整套设计的可用示例。',
    ].join('\n'),
  },
  {
    id: 'security',
    title: '安全与信任',
    body: [
      '这套信任模型值得直说，因为它有两个性质是刻意的，而其中一个很锋利。',
      '',
      '## 工具函数体就是代码执行',
      '',
      '工具函数体带完整内置函数编译并执行。它们**不做沙箱隔离**，因为自带的工具会导入 `requests`、SQLAlchemy 和应用内部模块，其中一个还会把模型写的 SQL 交给数据库——一个限制内置函数的沙箱会把它们全部弄坏。',
      '',
      '所以：编写工具等同于在服务器上执行代码。工具的管理接口必须只对管理员开放，`tools.body` 应该像审阅一次提交那样审阅。这也是**生成**按钮只起草函数体、不会静默安装它的原因。',
      '',
      '调用者仍然无法触及没有挂到自己智能体上的工具，模型也无法把 `project_id` 交给工具函数体——服务器提供这次运行自己的 id。',
      '',
      '## 访问控制',
      '',
      '| 规则 | 在哪里强制执行 |',
      '| --- | --- |',
      '| 管理员路由只对管理员开放 | 路由级依赖，没有哪个接口能漏掉 |',
      '| 注册不能造出管理员 | 角色在服务端设定，绝不从请求体读取 |',
      '| 聊天属于它的所有者 | 在 REST 路由上检查，在 WebSocket 上再检查一次 |',
      '| 聊天记录不能跨用户读取 | 非管理员总被过滤到自己的行 |',
      '| 登录和注册有频率限制 | 按客户端计键的滑动窗口限流器 |',
      '',
      '## 凭据',
      '',
      '- LLM API 密钥保存在数据库中，并在设置里**不做掩码**显示；该界面按设计只对管理员开放。',
      '- `SECRET_KEY` 用于签发 JWT。如果它还是示例值，应用会在启动时警告——在别人能访问这套部署之前改掉它。',
      '- 种子账号用的是种子密码。改掉它们。',
      '- `.env` 不要提交到仓库。请保持这样。',
      '',
      '## 模型输出在哪里被当作不可信',
      '',
      '- 表格导出会中和公式注入，因此以 `=` 开头的单元格不会在 Excel 中执行。',
      '- 工具结果在到达模型之前会被截断，而且是在 JSON 内部裁剪而不是直接切断。',
      '- 工作流运行在步数、工具轮数、委派深度和工具时长上都有上限，因此没有哪次运行能无限空转。',
    ].join('\n'),
  },
  {
    id: 'operations',
    title: '运维',
    body: [
      '## 运行',
      '',
      '```bash',
      '# 后端',
      'cd backend && venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000',
      '',
      '# 前端',
      'cd frontend/cerebrumkit-vue && npm run dev',
      '```',
      '',
      '在 Windows 上，`run.ps1` 启动两者，`stop.ps1` 停止它们。生产部署方面，`DEPLOY.md` 覆盖了 nginx、systemd，以及一条让数据库中的行（工具、技能、智能体）与代码保持同步的重建路径。',
      '',
      '## 数据库变更',
      '',
      '`backend/migrate.py` 会创建缺失的表并添加缺失的列。重复运行是安全的：新安装靠它建起来，`backend/app/models/` 里的新模型也靠它变成表。',
      '',
      '## 种子数据',
      '',
      '| 脚本 | 作用 |',
      '| --- | --- |',
      '| `seed_all.py` | 按顺序执行下面全部——新安装只需要这一条命令 |',
      '| `seed.py` | 根据 `SEED_*` 变量创建管理员和客户端账号 |',
      '| `seed_countries.py` | 国家列表 |',
      '| `seed_general_tools.py` | 通用工具及其技能 |',
      '| `seed_system_project.py` | 系统项目和 Project Manager |',
      '',
      '它们全都是幂等的——按名称匹配并就地更新——所以改过工具函数体之后再跑一次就会把修复推上去。',
      '',
      '## 在部署之间搬数据',
      '',
      '`export_for_deploy.py` 把项目数据写出去，`load_for_deploy.py` 把它读进来。用它们把工具、技能、智能体和存储定义从开发环境搬到已部署的环境。',
      '',
      '## 日志',
      '',
      '`run.ps1` 会把 `backend.log` / `backend.err.log` 和 `frontend.log` / `frontend.err.log` 写在项目根目录旁边。失败的运行会连着聊天 id 一起记录，所以一句「它没回答」可以追溯到那次失败的运行。',
      '',
      '## 部署智能体的改动',
      '',
      '大多数智能体改动根本不需要部署：在面板里改工具、技能或智能体，下一次运行就生效。只有**代码**变了才需要重新部署——智能体循环、某个路由、某个模型。',
    ].join('\n'),
  },
  {
    id: 'reference',
    title: '参考',
    body: [
      '## 上限',
      '',
      '每一道上限都是为了一次失控的运行不至于吃掉整个系统。它们位于 `backend/app/core/agent_loop.py`。',
      '',
      '| 上限 | 值 | 原因 |',
      '| --- | --- | --- |',
      '| 每个智能体轮次的工具调用轮数 | 100 | 防止模型在工具调用上打转 |',
      '| 被委派运行中的工具调用轮数 | 8 | 它必须在调用者的这一轮内作答 |',
      '| 委派深度 | 3 | 两个互相调用的智能体会停下 |',
      '| 工具调用超时 | 60 秒 | 永不返回的工具不能挂住整个运行 |',
      '| 委派调用超时 | 300 秒 | 嵌套运行需要比普通工具更长的时间 |',
      '| 交给模型的工具结果 | 8,000 字符 | 单个巨大的 API 响应不能吃掉上下文窗口 |',
      '| 聊天中保留的工具卡片 | 100,000 字符 | 卡片是给人看的，但同样有上限 |',
      '| 预运行上下文工具合计 | 24,000 字符 | 让系统提示词保持相称 |',
      '| 注入的智能体上一条回复 | 2,000 字符 | 够放结论，而不是整页 JSON |',
      '| 单次运行的工作流步数 | 200 | 畸形或成环工作流的兜底 |',
      '| 发送给智能体的历史聊天消息 | 0 | 事实由工具和记忆承载，而不是聊天记录 |',
      '| 模型调用超时 | 180 秒 | 每次调用、每个智能体 |',
      '',
      '## 数据表',
      '',
      '| 表 | 内容 |',
      '| --- | --- |',
      '| `users` | 账号、角色、语言、国家 |',
      '| `projects` | 项目、它们的工作流 JSON，以及 `is_system` 标记 |',
      '| `agents` | 智能体及其描述 |',
      '| `skills` | 技能及其指令文本 |',
      '| `tools` | 工具：函数规格（`description`）和 Python 函数体（`body`） |',
      '| `agent_skill`、`skill_tool` | 智能体有哪些技能、技能有哪些工具 |',
      '| `user_project`、`agent_project` | 项目成员关系 |',
      '| `agent_context_tools` | 预运行工具、它们的标题和参数 |',
      '| `chats`、`messages` | 对话及其中的每条消息，包括工具卡片 |',
      '| `memory` | 以（智能体，用户）为键的笔记 |',
      '| `project_tables` | 项目拥有哪些存储表 |',
      '| `platforms` | LLM 连接，以及哪一条处于激活 |',
      '| `countries` | 国家列表 |',
      '',
      '你在**存储**里创建的表也是真实的表，会和上面这些一起出现在这里。',
      '',
      '## 各个部分在哪里',
      '',
      '```text',
      'backend/app/core/       智能体循环、工具执行、委派、LLM 客户端、认证',
      'backend/app/models/     SQLAlchemy 模型',
      'backend/app/routers/    REST + WebSocket API',
      'backend/app/schemas/    请求与响应模型',
      'backend/app/system_management.py   Project Manager 使用的 manage_* 工具',
      'frontend/.../views/admin/    项目、技能、工具、存储、用户、设置、文档',
      'frontend/.../views/client/   项目与智能体聊天',
      'frontend/.../locales/        en、zh、ru、es、fr、de',
      '```',
      '',
      '## 添加一个业务领域',
      '',
      '在 `backend/app/models/` 下添加模型，在 `app/models/__init__.py` 中注册它们，用同样的方式添加路由和 schema，并用 `backend/migrate.py` 创建表。',
      '',
      '但更好的做法是优先使用存储库。项目在运行时需要的一切，都更适合做成**架在存储之上的工具**，因为这样拥有项目的人无需部署就能修改它——这正是工具和技能保存在数据库中的全部理由。',
    ].join('\n'),
  },
]